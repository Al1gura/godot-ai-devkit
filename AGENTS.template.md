# Godot 项目 AI 开发指令模板

将本模板复制为项目根目录的 `AGENTS.md` 并补全方括号。项目已经有指令时，只合并适用内容，不覆盖原文件。

## 项目定位

- 游戏/产品是什么：`[补充]`
- 主要玩家或用户：`[补充]`
- 最重要的真实使用情境或核心循环：`[补充]`
- 目标平台与主要输入方式：`[补充]`
- 明确不做：`[补充]`
- Godot 精确版本：`[补充]`
- 主要语言：`[GDScript / C# / 混合]`
- 自动测试入口：`[补充；尚未建立时写“无”]`
- 状态/开发日志入口：`[补充；一次性原型可写“无”]`

## 开发套件入口

- 开始 Godot 工作前读取 `godot-ai-devkit/SKILL.md`。
- 根据总技能的任务表，只读取当前需要的 `godot-ai-devkit/skills/*/SKILL.md`，不要一次加载全部。
- 项目使用 Godot 4.7 时，不确定 API 必须搜索 `godot-ai-devkit/references/godot-4.7-docs/`；其他版本使用匹配版本官方资料，不能拿 4.7 文档证明不同版本行为。

## 产品和范围

- 先确认玩家或用户要完成的结果和最短操作路径，再设计系统、数据和界面。
- 不把技术模块自动变成独立面板；每个新界面和控件必须对应真实用户任务。
- 以完成当前目标所需的最小改动为优先；推测不能授权扩展功能或顺便重构。
- 复杂系统先跑通一个真实端到端案例，再提炼通用结构。
- 保留无关未提交改动；不覆盖、回退或清理不属于当前目标的文件。

## 现成方案优先

- 技术选择前主动搜索，不等用户提醒。
- 依次检查项目现有实现、目标版本 Godot 官方能力、维护中的插件/开源方案和必要的同版本引擎源码；都不适用时才自行编写。
- 采用外部方案前检查 Godot 版本、许可证、维护状态、平台、依赖、范围和项目冲突。
- 已有同版本、同行为链的有效依据直接复用，不为流程形式重复调查。

## 代码与数据

- 项目现有语言和风格优先。新建长期代码默认写清类型；不为统一风格改写无关旧代码。
- GDScript 文件、函数和变量使用 `snake_case`，类和节点使用 `PascalCase`，常量使用 `UPPER_SNAKE_CASE`，私有成员使用 `_` 前缀；项目另有规定时服从项目。
- 不确定 Godot 接口前核对目标精确版本资料，不混用 Godot 3 与 Godot 4 写法。
- 优先简单、可读、符合当前用途的结构；不预建没有真实使用案例的万能框架。
- 测试使用隔离数据，不读取、修改或删除正式存档和用户资源。

## 验证

- 自动测试证明确定性逻辑；运行态检查证明当前实例状态；原生可见窗口证明真实渲染和操作系统输入；用户产品验收证明流程是否易懂好用。它们不能互相冒充。
- 涉及渲染、真实输入、窗口、多显示器、音视频、分辨率/缩放或图形处理器行为时，做原生可见窗口验证。
- 新增或改变高频多步骤流程、关键文案、恢复路径、真实设备环境或主观可读性时，在最近的稳定端到端成果安排用户产品验收。
- 自动化能完成的技术验证不转嫁给用户；内部重构和纯数据工作不要求用户手测。

## 状态记录和完成

- 日志不按大小阶段机械更新，只记录可交接状态变化：用户结果可用、重要决定改变、根因确认/修复、合同改变、未解决风险或会话交接。
- 同一连续修改中的保存、测试重跑和内部拆分不单独记录；父子阶段不重复同一事实。
- 只有用户结果在承诺范围内可用、所需程序验证完成、剩余风险公开时，才宣布完成。
- 完成报告先说用户获得的结果，再简要说明改动位置、未做内容、直接相关证据和是否需要产品验收。
