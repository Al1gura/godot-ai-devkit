# Godot 项目 AI 开发指令模板

将本模板中适用的部分合并进项目自己的 AI 指令。不要直接覆盖已有项目规则。

## 项目定位

- 产品/游戏是什么：`[补充]`
- 主要玩家或用户：`[补充]`
- 最重要的真实使用情境：`[补充]`
- 明确不做：`[补充]`
- Godot 精确版本：`[补充]`

## 工作原则

- 先确认玩家或用户要完成的结果和最短操作路径，再设计系统、数据和界面。
- 不把技术模块自动变成独立面板。每个新界面和控件必须对应真实用户任务。
- 以完成当前目标所需的最小改动为优先；推测不能授权扩展功能或顺便重构。
- 保留用户已有的无关改动；不覆盖、回退或清理不属于当前目标的文件。
- 修复缺陷前先取得真实报错、日志、坏状态或可重复操作。

## 现成方案优先

- 选择实现方式前主动搜索，不等用户提醒。
- 依次检查项目现有实现、目标版本 Godot 官方能力与示例、维护中的插件/开源实现、必要时的同版本引擎源码；都不适用时才自行编写。
- 采用外部方案前检查 Godot 版本、许可证、维护状态、平台、依赖、范围和与现有项目的冲突。
- 调研深度与风险相称；已有有效证据直接复用，不为流程形式重复调查。

## Godot 代码

- 项目现有语言和风格优先。新建长期代码默认写清类型；不为统一风格改写无关旧代码。
- GDScript 文件、函数和变量使用 `snake_case`，类和节点使用 `PascalCase`，常量使用 `UPPER_SNAKE_CASE`，私有成员使用 `_` 前缀；项目另有规定时服从项目。
- 不确定 Godot 接口前核对目标精确版本官方资料，不混用 Godot 3 与 Godot 4 写法。
- 优先简单、可读、符合当前用途的结构；不预先建设尚无真实使用案例的通用框架。
- 数据、规则、表现和用户界面按实际变化原因分离，但不为分层本身增加空壳类或界面。

## 验证

- 自动测试证明确定性逻辑；运行态检查证明当前实例状态；原生可见窗口证明真实渲染和操作系统输入；用户产品验收证明流程是否易懂好用。它们不能互相冒充。
- 只有涉及渲染、真实输入、窗口、多显示器、音视频、分辨率/缩放或图形处理器行为时，才必须做原生可见窗口验证。
- 新增或改变高频多步骤流程、关键文案、恢复路径、投屏/硬件环境或主观可读性时，在最近的稳定端到端成果安排用户产品验收。
- 自动化能完成的技术验证不转嫁给用户；内部重构和纯数据工作不要求用户手测。

## 状态记录

- 日志不按大小阶段机械更新，只记录可交接的状态变化：用户结果可用、重要决定改变、根因确认/修复、合同改变、未解决风险或会话交接。
- 同一连续修改过程中的每次保存、测试重跑和内部拆分不单独记；父子阶段不重复记录同一事实。
- 一次性小原型可以不建日志；跨会话、协作、长期维护或高风险项目保留唯一状态/日志入口。

## 完成报告

- 先说用户获得的结果，再简要说明改动位置、未做内容、直接相关的程序证据和是否需要用户产品验收。
- 只有用户结果在承诺范围内可用、所需程序验证完成、剩余风险已公开时，才宣布完成。
