---
name: godot-ai-devkit
description: 以真实玩家或用户目标为起点开发、修改、调试和审核 Godot 4 项目；主动寻找并评估项目现有实现、Godot 官方能力、插件和开源方案，避免重复造轮子；遵守项目代码规范，按风险选择自动测试、运行态、真实窗口和用户产品验收。用于任何需要编写或修改 GDScript、C#、Godot 场景、资源、界面、交互或项目架构的任务。
---

# Godot AI Development

把工作视为向玩家或用户交付可用体验，而不是堆叠系统、类、数据字段和界面。

## 1. 从真实使用目标开始

开始设计或修改前，先用项目材料和用户请求回答：

- 谁会使用它？
- 他在什么情境下想完成什么？
- 最短、最自然的操作路径是什么？
- 哪些内容明确不需要？

不要把“存在一个技术系统”自动翻译成“需要一个独立面板、列表、详情页和确认弹窗”。新增的每个可见控件都必须对应用户需要作出的决定或动作。

设计新功能、改变工作流、增加界面或用户质疑复杂度时，完整读取 [references/product_first.md](references/product_first.md)。

## 2. 建立项目事实

1. 读取项目自己的指令、产品目标、当前状态、相关代码/场景/资源、测试和 Git 状态。
2. 项目规则与本技能冲突时，以更具体的项目规则为准。
3. 区分用户明确要求的结果与推测出的改进；推测不能授权扩展功能。
4. 修复缺陷前先取得真实报错、日志、坏状态或可重复操作，不用猜测代替证据。
5. 保留无关的未提交改动，不覆盖、回退或顺手整理用户的其他工作。

## 3. 实现前主动复用

任何需要选择实现方式的新增功能或不确定修改，都先寻找成熟方案，不以用户是否说出“照 Godot 做”或“找插件”为触发条件。

按顺序检查：

1. 当前项目里已经验证的实现和约定；
2. 目标 Godot 精确版本的公开接口、官方文档、演示和内置能力；
3. Asset Library（资源库）、GitHub 等来源中仍维护且许可证合适的插件或开源实现；
4. Godot 引擎源码中与内部生命周期或编辑器行为有关的实现；
5. 只有前面方案不适用时才自行编写，并说明原因。

调研深度与风险相称。普通公开接口不需要研究整个引擎；插件也不因为“现成”就自动优于小型本地实现。需要选择新方案、依赖或复杂交互时，完整读取 [references/reuse_first.md](references/reuse_first.md)。

## 4. 设计最小可用改动

1. 先描述用户完成目标的最短路径，再决定数据和技术结构。
2. 优先扩展现有入口；没有独立用户任务时不新增独立界面。
3. 只引入当前目标需要的状态、抽象和配置。
4. 复杂系统先完成一个真实、端到端的成功案例，再从已验证实现提炼通用结构。
5. 架构分层不得让用户承担额外步骤；技术模块数量不等于界面数量。

## 5. 按项目规范实现

遵循项目现有语言、类型、命名、目录和架构风格。新建且长期维护的 GDScript 默认写清参数、返回值和重要变量类型；不要为风格统一批量迁移无关旧代码。项目规定更严格时严格遵守。

写或审查 Godot 代码前完整读取 [references/godot_code_style.md](references/godot_code_style.md)。不确定接口名称、属性类型或生命周期时，核对目标版本官方资料，不凭记忆混用 Godot 3 和 Godot 4 写法。

## 6. 按风险验证

使用能覆盖真实风险的最小证据组合：

- 文档和静态配置：格式、链接和一致性检查；
- 确定性逻辑：可重复自动测试；
- 场景树、信号、生命周期和实时状态：增加运行态检查；
- 渲染、真实输入、窗口、多显示器、音视频和图形处理器行为：增加原生可见窗口验证；
- 可发现性、文案、高频多步骤工作流、恢复路径和主观可读性：在最近的稳定端到端成果安排用户产品验收。

完整矩阵见 [references/verification.md](references/verification.md)。运行通道不可用时，可以继续不依赖它的工作，但不得把缺失证据写成通过。

## 7. 在可交接状态变化时记录

日志不跟随项目如何命名“大阶段、小阶段、子阶段”更新。只在仓库状态发生了对后来者有意义的变化时更新项目约定的状态或日志文件，例如：

- 一个用户结果达到可用、可验证或可交接状态；
- 重要产品或技术决定被确定、改变或撤销；
- 缺陷根因被确认或修复；
- 接口、数据格式、操作方法或验证合同改变；
- 出现尚未解决的阻塞、风险或失败证据；
- 会话结束或交接时仍有未完成上下文。

同一连续修改过程中的每次文件保存、测试重跑和内部拆分不单独记日志。父阶段和子阶段不要重复记录同一事实。一次工作只有在用户要求的结果于承诺范围内可用、所需程序证据完成、剩余风险已公开时，才可以宣布完成；阶段名称和层级不能代替这个判断。

一次性试验或可随时丢弃的小原型可以不建日志。跨会话、多人或 AI 协作、长期维护、数据风险较高的项目应保留一个唯一的状态/日志入口。

## 8. 简短交付

完成时说明：

- 用户现在获得了什么；
- 改了哪些文件或行为；
- 哪些内容没改以及原因；
- 与本次风险直接相关的验证结果；
- 是否需要用户产品验收，以及它验证什么。

完整工作顺序见 [references/workflow.md](references/workflow.md)。排错时读取 [references/debugging.md](references/debugging.md)。
