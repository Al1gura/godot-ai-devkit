---
name: godot-ai-devkit
description: 以真实玩家或用户目标为起点开发、修改、调试和审核 Godot 4 项目；主动复用项目现有实现、Godot 官方能力、插件和开源方案；从随包 GodotPrompter 的 54 个专业 Skill 中按任务选择项目启动、GDScript、架构、玩法、界面、输入、调试、测试、2D、3D、发布或其他能力；使用 Godot 4.7 离线文档核对 API，并按风险验证结果。用于任何需要创建或修改 Godot 项目、代码、场景、资源、界面、交互或架构的任务。
---

# Godot AI Development

把工作视为向玩家或用户交付可用体验，而不是堆叠系统、类、字段和界面。

## 开始任何任务

1. 先完整读取同目录 `README.md`，把它作为不同 AI 共用的启动入口。
2. 读取同目录 `AGENTS.md` 以及项目自己的 `AGENTS.md`、产品目标、当前状态、相关文件、测试和 Git 状态。长期项目没有根 `AGENTS.md` 时，由 AI 根据已确认事实创建简短的项目专用指令并指向本开发包；已有时同时遵守且以更具体的项目规则为准，不要求用户复制、改名或填写模板。
3. 确认目标用户、真实情境、期望结果、最短操作路径和明确不做的范围。
4. 项目规则与本技能冲突时，以更具体的项目规则为准。
5. 保留用户的无关改动；推测不能授权增加功能或顺便重构。
6. 缺陷先取得真实报错、日志、坏状态或可重复操作，不用猜测代替证据。

设计新功能、改变用户流程或新增界面时读取 [产品优先](references/product_first.md)。

## 主动寻找现成方案

只要实现需要作技术选择，就依次检查：

1. 当前项目已验证的实现和约定；
2. 目标 Godot 精确版本的内置能力、官方文档和演示；
3. Asset Library（资源库）、GitHub 等来源中仍维护且许可证合适的方案；
4. 行为依赖未公开生命周期、导入/缓存或编辑器内部机制时，查看同版本引擎源码；
5. 都不适用时才写覆盖当前成功案例的最小实现，并说明原因。

完整选择规则见 [现成方案优先](references/reuse_first.md)。不因为插件“现成”就忽略版本、维护、许可证、平台、范围和冲突。

需要测试框架、运行态接口、代码检查或功能插件时，读取 README 的“Gvtt 已验证的工具经验”。先检查目标项目已有能力，只在当前任务需要时说明理由并取得用户授权；不要默认安装 Gvtt 出现过的全部工具。

## 按任务选择专业技能

1. 先完整读取 [GodotPrompter 技能目录](skills/using-godot-prompter/SKILL.md)，用其中的任务映射确认当前需要哪些 Skill。
2. 只读取当前任务直接相关的 Skill 正文；一个任务可以组合多个，但不要预先加载全部 54 个。
3. 项目已有更具体的 Skill、合同和实现时优先使用；GodotPrompter 提供领域方法，不覆盖项目事实。
4. 第三方插件 Skill（例如 LimboAI、Beehave、Dialogue Manager、Popochiu、Phantom Camera）只有项目确实采用对应插件时才读取，不能把 Skill 的存在当成安装插件的授权。
5. GodotPrompter 面向 Godot 4.3+ 并包含分版本说明。项目使用 4.7 时，最终 API 结论仍须由随包 4.7 官方离线文档确认；其他版本使用对应版本官方资料。
6. GodotPrompter 的测试 Skill 会介绍 GUT 和 gdUnit4，但目标项目已有测试方式时不得擅自替换；是否引入外部测试框架按 README 的工具经验和用户授权决定。

## 使用离线文档

先从 `project.godot`、编辑器信息或项目规则确认精确 Godot 版本。

项目使用 Godot 4.7 时，不确定类、方法、属性、信号、枚举、生命周期或 Godot 3→4 改名，先用文本搜索定位：

```text
references/godot-4.7-docs/
```

优先搜索精确类名和成员名，再读取命中附近上下文；不要把 1,593 份文档全部加载进提示。项目不是 4.7 时，这份文档只能帮助发现关键词，最终结论必须由目标版本官方资料确认。

## 设计最小端到端改动

1. 先描述用户从入口到结果的最短路径，再决定技术结构。
2. 优先扩展现有入口；没有独立用户任务时不新增独立面板。
3. 只引入当前目标需要的状态、抽象和配置。
4. 复杂系统先完成一个真实成功案例，再从已验证实现提炼通用结构。
5. 技术模块数量不等于界面数量，架构分层不能增加用户步骤。

通用代码要求见 [Godot 代码规范](references/godot_code_style.md)。

## 按风险验证

- 文档和静态配置：检查格式、链接和一致性；
- 确定性逻辑：使用可重复自动测试；
- 场景树、信号、生命周期和实时状态：增加运行态检查；
- 渲染、真实输入、窗口、多显示器、音视频和图形处理器行为：增加原生可见窗口验证；
- 可发现性、文案、高频多步骤流程、恢复路径和主观可读性：在最近的稳定端到端成果安排用户产品验收。

完整矩阵见 [验证要求](references/verification.md)。一种证据不能冒充另一种，缺少的证据不得写成通过。

## 记录可交接状态

日志不跟随“大阶段、小阶段、子阶段”的命名更新。只在仓库出现对后来者有持续价值的新事实时记录：用户结果达到可用/可验证状态、重要决定改变、根因确认或修复、合同改变、未解决风险，或会话交接仍有上下文。

同一连续修改中的保存、测试重跑和内部拆分不单独记录；父子阶段不重复同一事实。一次性可丢弃原型可以不建日志；跨会话、协作、长期维护或高风险项目保留一个唯一状态入口。

## 完成条件

只有用户要求的结果在承诺范围内可用、所需程序证据完成、剩余风险公开时，才宣布完成。完成报告只需说明用户获得什么、改动位置、未做内容、直接相关证据和是否需要产品验收。

完整顺序见 [通用工作流](references/workflow.md)。排错经验见 [错误与排错](references/debugging.md)。
