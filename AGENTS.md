# Godot 项目 AI 开发指令

本文件是直接生效的通用规则，不是让用户复制或改名的模板。AI 读取本开发包后必须遵守；目标项目已有更具体的 `AGENTS.md` 时，以项目规则为准，本文件补足其未规定的部分。

## 接手项目

1. 完整读取同目录 `README.md` 和 `SKILL.md`，再读取 `skills/using-godot-prompter/SKILL.md` 的技能目录，按当前任务选择相关细分技能；不要一次加载全部 54 个 GodotPrompter Skill 正文。需要 Godot 运行态证据时再读取 `skills/gdmcp/SKILL.md`。
2. 检查目标项目的实际文件、Git 状态、`project.godot`、已有项目指令、文档入口、相关代码和测试，不凭文件名猜项目状态。
3. 从现有材料确认产品是什么、服务谁、核心使用情境、目标平台、精确 Godot 版本、主要语言、明确不做和当前优先级。
4. 只有会改变产品方向且无法从项目发现的信息才询问用户；技术名词、文件归类和验证方法由 AI 负责。
5. 长期项目若没有项目根 `AGENTS.md`，由 AI 根据已经确认的项目事实创建一份简短的项目专用指令，并指向本开发包；不让用户复制、改名或填写模板。一次性原型不强制创建。

## MCP 开发基线

- 用户把本开发包交给 AI，即授权 AI 在当前项目内配置随包 Godot-MCP-Native 1.0.8；不需要用户手动复制、改名或再次确认普通项目文件写入。
- 配置前检查项目根 `addons/godot_mcp/`、`.gdmcp/`、`project.godot` 的插件启用项、精确 Godot 版本和端口约定。已有不同版本、本地修改或同名端口冲突时不得覆盖，先报告差异和影响。
- 无冲突时，从本包复制 `addons/godot_mcp/` 到项目根同一路径；Windows x86_64 同时复制 `.gdmcp/bin/gdmcp.exe`。安全合并 `project.godot` 的 `res://addons/godot_mcp/plugin.cfg` 启用项，不删除或重排其他插件。
- macOS 与 Linux 复用随包 Godot 插件，但必须从 Godot-MCP-Native v1.0.8 发布页取得对应平台的 `gdmcp` 并校验发布指纹；不得尝试运行 Windows EXE。
- 默认 HTTP（网页传输）端口为 `9080`。项目另有端口时以项目为准，并把实际 MCP 地址写入项目开发指令或现有工具链文档；不得把某个项目的自定义端口推广为通用默认值。
- 启动编辑器后，命令行型 AI 读取 `skills/gdmcp/SKILL.md`，优先调用项目根 `.gdmcp/bin/gdmcp.exe`，先运行 `doctor` 和 `editor state`。项目定义了地址时每条命令显式传入该地址；存在 EXE 或插件目录不等于连接成功。
- MCP 只在运行态证据能降低风险时连接。纯文档、静态检查、普通文件操作不为形式启动；场景树、节点、信号、输入、编辑器/游戏日志和实时状态需要真实证据时使用。MCP 不能替代可重复自动测试，自动测试也不能冒充当前实例的运行态证据。

## 每次任务读取什么

开始任务时按下面顺序读取，已有同等职责文件时使用现有文件名，不强制项目改名：

1. 项目根 `AGENTS.md` 和本文件：长期规则、禁区、工具与验证纪律。
2. 项目文档索引：确认当前真值文件、历史文件和阅读顺序。
3. 产品文档与当前路线图：确认用户价值、范围、当前优先级和明确不做。
4. 与当前任务直接相关的架构、功能合同、源文件和测试。
5. 修改 Godot 接口前核对项目精确版本的官方文档；使用 Godot 4.7 时可搜索随包离线文档。
6. 修复缺陷时再读取相关日志、失败证据和历史问题；不要把整份开发日志当作每次任务的固定前置上下文。

只读到足以安全完成当前任务为止。不要为“全面了解”加载所有文档、技能、离线手册或历史记录。

## 文档职责与写入位置

项目可以沿用自己的文件名和目录，但每种事实必须有唯一主位置。没有现有结构的长期项目，使用下面的最小结构：

| 内容 | 默认主位置 | 什么时候写 |
|---|---|---|
| 给人看的项目介绍、安装、运行、导出和使用方法 | `README.md` | 人的操作方式发生变化时 |
| AI 长期开发规则、命名、命令、工具、禁区和验证纪律 | `AGENTS.md` | 规则会影响以后多数任务时 |
| 文档地图、当前真值、阅读顺序和历史档案标识 | `docs/README.md` | 新增、替换、移动或归档文档时 |
| 目标用户、核心体验、产品边界和明确不做 | `docs/product.md`；已有 `docs/design.md` 时沿用 | 产品决定改变时 |
| 已实现架构、数据所有权、生命周期、模块边界和代码入口 | `docs/architecture.md` | 实际实现或边界改变时；不写尚未实现的愿望 |
| 当前优先级、阶段依赖和最近产品验收目标 | `docs/roadmap.md` | 优先级、阶段状态或验收目标改变时 |
| 复杂、高风险或跨阶段功能的行为合同 | `docs/contracts/<feature>.md` | 需要跨任务保持边界、状态或验收口径时；简单功能不新建 |
| 已完成结果、重要决定变化、失败证据和未解决风险 | `devlog/DEVLOG.md` | 出现可交接状态变化时 |
| 可重复的确定性行为合同 | `tests/` | 新行为、修复或回归风险需要程序保护时 |
| 少数长期且难以逆转的技术选择及取舍 | `docs/decisions/` | 项目确实需要追溯重大决定时；小项目不强制 |

“默认主位置”是职责，不是强制文件名。项目已有 `design.md`、阶段计划或专题合同等有效结构时，先映射职责并继续沿用，不为统一外观批量改名。

## 文档更新判断

任务结束前逐项判断“事实是否改变”，只更新发生变化的主文档：

- 用户操作方式改变：更新人的 `README.md`。
- 长期开发纪律或固定命令改变：更新项目 `AGENTS.md`。
- 产品范围或用户目标改变：更新产品文档。
- 已实现模块、数据流或边界改变：更新架构文档。
- 当前优先级、阶段状态或验收目标改变：更新路线图。
- 复杂功能的外部行为、状态合同或验收口径改变：更新对应合同。
- 结果已经可交接、重要决定改变、根因确认/修复或仍有风险：更新开发日志。
- 只有代码内部整理且外部合同未变：通常不改产品、路线图或合同；按风险更新测试。
- 只有重复跑测试、保存文件或同一任务内部拆分：不单独写日志。

不要按“大阶段、小阶段、子阶段”的名称机械更新日志。判断标准是后来接手的人是否会因为缺少这条新事实而误判状态。

## 单一真值与历史

- 同一事实只在一个主文档完整描述，其他文件只链接或用一句话指向，不复制整段内容。
- 当前计划和已实现架构必须分开；路线图不能冒充架构，架构不能把计划写成已完成。
- 开发日志记录发生了什么和证据，不重复设计全文，也不代替当前状态文档。
- 旧计划、旧交接和阶段复盘失效后标为历史或移入档案；文档索引必须说明当前应读哪一份。
- 多份文件冲突时先依据代码、测试、用户最新决定和运行证据确认真值，再修正文档；不得任选一份继续。
- 简单项目只需 `README.md`、`AGENTS.md` 和必要测试；产品、架构、路线图等文件只在确有持续信息时创建。

## 产品和范围

- 先确认玩家或用户要完成的结果和最短操作路径，再设计系统、数据和界面。
- 不把技术模块自动变成独立面板；每个新界面和控件必须对应真实用户任务。
- 新增独立能力或明显扩大既有能力时，写第一行实现代码前先确定状态、行为、界面和需要时的保存职责由谁自然拥有，以及组合根只需连接的窄接口；不需要持久化时明确不保存。主场景脚本／应用根只负责装配模块、注入依赖、连接跨模块事件和顶层生命周期；不得先接收功能私有状态、内部规则、完整界面树、专属事件处理器或序列化细节，再承诺以后拆分。小型紧耦合辅助逻辑可以留在已有所有者中；不按行数机械拆文件，也不强制每项能力预建 Controller／Service／View（控制器／服务／界面）全套结构。
- 正式、稳定且具有独立职责、明显复杂度、复用或独立测试价值的界面，优先以 `.tscn` 场景组件表达结构、布局和初始状态；父场景负责组合，脚本负责数据绑定、状态与信号。拆分收益必须大于新增文件和连接成本；小型紧耦合的一次性结构可以留在父场景，小型临时反馈或无稳定布局的纯动态内容可以代码生成。完整多区域界面不得持续堆进单个 `_build_*_ui()` 脚本。
- 以完成当前目标所需的最小改动为优先；推测不能授权扩展功能或顺便重构。
- 复杂系统先跑通一个真实端到端案例，再从已验证实现提炼通用结构。
- 保留无关未提交改动；不覆盖、回退或清理不属于当前目标的文件。

## 现成方案优先

- 技术选择前主动搜索，不等用户提醒。
- 依次检查项目现有实现、目标版本 Godot 官方能力、维护中的插件或开源方案，以及必要的同版本引擎源码；都不适用时才自行编写。
- 采用外部方案前检查 Godot 版本、许可证、维护状态、平台、依赖、范围和项目冲突。
- 已有同版本、同行为链的有效依据直接复用，不为流程形式重复调查。

## 代码、数据和验证

- 项目现有语言和风格优先。新建长期代码默认写清类型；不为统一风格改写无关旧代码。
- GDScript 文件、函数和变量使用 `snake_case`，类和节点使用 `PascalCase`，常量使用 `UPPER_SNAKE_CASE`，私有成员使用 `_` 前缀；项目另有规定时服从项目。
- 不确定 Godot 接口前核对目标精确版本资料，不混用 Godot 3 与 Godot 4 写法。
- 测试使用隔离数据，不读取、修改或删除正式存档和用户资源。
- 自动测试证明确定性逻辑；运行态检查证明当前实例状态；原生可见窗口证明真实渲染和操作系统输入；用户产品验收证明流程是否易懂好用。它们不能互相冒充。
- 新增或大改正式界面时，先明确场景树与组件边界及不拆分理由；验证承担独立职责的稳定区域来自 `.tscn`，父脚本没有重新创建其内部节点树。数据驱动列表验证容器来自场景、重复项由组件模板实例化。纯静态小结构可以由父场景加载和窗口验证覆盖，不强制每个文件单独测试。
- 自动化能完成的技术验证不转嫁给用户；内部重构和纯数据工作不要求用户手测。

## 完成

只有用户结果在承诺范围内可用、所需程序验证完成、相关文档已按职责更新且剩余风险公开时，才宣布完成。完成报告先说用户获得的结果，再简要说明改动位置、未做内容、直接相关证据和是否需要产品验收。
