# 通用开发流程

## 1. 定义真实结果

- 说明目标用户、使用情境、期望结果和最短路径。
- 明确当前请求的边界以及不做的内容。
- 如果现有设计已经让简单目标变复杂，先减少步骤和界面，再讨论增加功能。

## 2. 建立当前事实

- 读取项目规则、产品文档、相关代码/场景/资源、测试和当前 Git 状态。
- 对比文档与实际实现；有矛盾就公开指出。
- 缺陷先复现或取得真实坏状态，再决定修改位置。
- 判断是否需要工作文档：简单任务使用当前任务记录；只有复杂、跨会话或多人协作任务才建立临时计划。需要长期约束行为、状态或验收口径时，直接建立或更新正式合同。

## 3. 搜索可复用方案

- 先找项目现有模式，再找 Godot 官方能力和维护中的开源方案。
- 只对新增不确定性调查；已经验证且条件未变的结论直接复用。
- 记录采用、不采用和自行实现的理由，避免后来者重复调查。

## 4. 实现最小端到端改动

- 先让一个可信用户案例从入口到结果完整跑通。
- 只增加该案例实际需要的数据、状态、界面和抽象。
- 保持项目现有风格，保护无关改动和用户数据。
- 尚未实现的方案留在计划或路线图，不提前写进“已实现架构”；已经确定且立即生效的产品决定或合同可以在开发中更新正式文档。
- 遇到会改变产品方向、破坏数据或需要新权限的分叉才暂停询问。

## 5. 按风险验证

1. 静态检查：解析、格式、链接、配置和差异检查。
2. 专项自动测试：证明确定性逻辑和修复目标。
3. 相邻回归：只覆盖共享同一合同的邻近行为。
4. 运行态检查：涉及节点、信号、生命周期、输入或实时状态时使用。
5. 原生可见窗口：涉及真实渲染、操作系统输入/窗口、多显示器、音视频、分辨率缩放或图形处理器时使用。
6. 用户产品验收：在稳定端到端成果上验证可发现性、文案、操作负担、恢复和主观体验。

验证前为本轮隔离目录、日志、截图、转储、工程副本和下载指定生成根目录与保留类别。不要先生成，最后才猜哪些文件属于当前任务。

## 6. 收束生成产物

1. 从本轮产物中提取测试结果和必要证据。
2. 成功运行的普通临时产物默认删除；失败只保留复现所需的最小日志、截图或转储，并服从项目的次数、期限或体积上限。
3. 正式基线移入明确的长期位置；`.godot/` 等可重建缓存与用户／项目资产不按临时目录处理。
4. 只回收本任务创建且位于已声明生成根目录内的文件和进程，不清理未知内容或用户已有 Godot 实例。
5. 本轮确实生成过磁盘产物时，完成前复核清理后体积、残留进程和有意保留的证据及原因；超出项目产物上限时停止继续生成并先处理。

## 7. 收束文档并记录可交接状态

完成时不是把功能文档全文“合并”进几个固定文件，而是只提炼仍有长期价值的新事实：产品决定、真实架构、当前计划、持久合同、结果证据和遗留风险分别进入其唯一主位置。实现细节继续由代码和测试承担，不在文档重复抄写。

临时计划完成后必须选择一个出口：删除；移入明确的历史档案并从当前阅读入口移除；或明确晋升为一份仍有长期职责的正式文档。简单功能没有临时文档就不需要制造这一流程。

不要问“这是大阶段还是小阶段”，改问：

> 如果现在换一个不了解本次对话的人接手，仓库现有记录是否足以让他知道结果、决定、证据和剩余风险？

答案为否且状态已经具有持续价值时，更新唯一日志/状态入口。一次连续修改只在形成新事实时记录，不按内部步骤重复追加。

## 8. 报告

先说用户获得的结果，再报告改动位置、未做内容、直接相关的验证和剩余风险。程序验证与用户产品验收分开表述。
